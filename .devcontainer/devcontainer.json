{
  "name": "Prompt-paper",
  "dockerFile": "Dockerfile",
  "workspaceFolder": "/workspace",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind"
  ],
  "runArgs": [
      "--gpus", "all",
      "--shm-size", "6G"
  ],
  "postStartCommand": "python -m pip install --upgrade pip; pip install black; pip install -e ." //; pip install -e .
}