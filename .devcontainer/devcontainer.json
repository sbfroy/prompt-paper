{
  "name": "GRaSp",
  "dockerFile": "Dockerfile",
  "workspaceFolder": "/workspace",
  "mounts": [
    "source=${localWorkspaceFolder},target=/workspace,type=bind"
  ],
  "runArgs": [
    "${localEnv:USE_GPU:+--gpus}",
    "${localEnv:USE_GPU:+all}",
    "--shm-size", "6G"
  ],
  "postStartCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt && pip install black -e ."
}
